# Claude Code 工作约定

> **Claude Code(或任何 AI 协作者)初次接入本项目时**,先看这份文档。

---

## 必读顺序(15 分钟)

1. **本文件**(`.claude_code_init.md`)— 5 分钟
2. **`docs/HANDOFF.md`** — 5 分钟,理解项目背景 + 决策
3. **`docs/DEVELOPMENT_PLAN.md` 第 Week 1-2 节** — 5 分钟,理解当前任务

---

## 用户工作风格(必须遵守)

### 1. 一步一步,不要批量

❌ 不允许 一次实现 5 个文件 / 5 个功能
✅ 必须 实现 1 个 → 跟用户确认 → 用户说"继续" → 下一个

**例外**:文件之间强相关无意义拆分时(如 `pyproject.toml` + `requirements.txt`),可以一次出。但要明说"这两个一起出"。

### 2. 设计先行,代码后行

涉及任何"决策"性质的工作时:
- 先用 markdown / 表格说明设计
- 解释为什么这么做
- 用户认可 → 才写代码

### 3. 三检验证

任何 bug 修复 / 新功能,**至少 3 个 checkpoint**:
- Checkpoint 1: 代码 / 文件结构正确
- Checkpoint 2: 功能输出符合预期
- Checkpoint 3: 边界情况测试

### 4. 根因分析

遇到 bug:
- ❌ 不是"修了就行"
- ✅ 必须找出根因 + 解释为什么会发生 + 怎么防止再次出现

### 5. 不强迫用户回答问题

- ❌ 用 ask_user_input_v0 弹强制选项
- ✅ 用文字简短说有什么问题 + 继续做事
- 用户想答就答

---

## 编码规范

### Python

```python
# 严格类型注解
async def get_funding_rate(
    self,
    symbol: str,
    *,
    timeout: float = 5.0
) -> FundingRate:
    """获取资金费率"""
    pass

# 异常处理:具体类型
try:
    ...
except BinanceAPIError as e:  # 不用 Exception
    logger.error("API 错误", error=str(e))
    raise

# 用 structlog,不用 logging
import structlog
logger = structlog.get_logger(__name__)

# 用 pydantic v2 数据模型
from pydantic import BaseModel, Field
from decimal import Decimal

class Position(BaseModel):
    symbol: str
    size: Decimal  # 用 Decimal 表示金额,绝不用 float
    entry_price: Decimal

# 用 asyncio,异步优先
async def main():
    ...
```

### 关键禁忌

```
❌ 用 float 表示金额 / 价格(必须 Decimal)
❌ 用 print 输出(必须 structlog)
❌ try: except Exception:(必须具体异常)
❌ 在循环里 await(用 asyncio.gather)
❌ 同步 IO 阻塞 event loop
❌ 把 secrets 写在代码里
❌ 用 f-string 拼 SQL(用参数化)
```

### Git 规范

```bash
# 提交信息格式
<type>: <subject>

<body>

# type 包括:
feat:     新功能
fix:      bug 修复
docs:     文档
refactor: 重构
test:     测试
chore:    杂项

# 例子:
feat: 实现 Binance 资金费率扫描器

- 5 分钟扫描所有合约 funding rate
- 入库到 funding_history 表
- 添加 Redis pub/sub 推送
- 单元测试覆盖率 85%
```

### 测试要求

```
- 核心模块:单元测试覆盖率 > 80%
- 风控引擎:单元测试覆盖率 > 95%(关键命门)
- 集成测试:用 binance testnet
- 回测引擎:对照真实成交验证误差 < 5%
```

---

## 项目结构约定

```
Dracula-System/
├── backend/
│   ├── app/
│   │   ├── core/        # 配置、日志、依赖
│   │   ├── exchanges/   # 交易所适配器
│   │   ├── strategies/  # 策略实现
│   │   ├── risk/        # 风控引擎
│   │   ├── execution/   # 订单执行
│   │   ├── notifications/
│   │   ├── api/         # FastAPI
│   │   └── models/      # 数据模型
│   ├── tests/
│   ├── scripts/
│   └── pyproject.toml
├── frontend/            # Next.js(暂未开始)
├── docs/                # 设计文档(只增不删)
├── prototypes/          # UI 原型
├── config/              # 配置文件
│   ├── config.yaml
│   └── strategies/
├── docker-compose.yml
├── .env.example
└── README.md
```

---

## 安全约束(绝对不能违反)

```
❌ API key / secret 进 Git
❌ 真实 API key 硬编码
❌ withdraw 权限保留(必须关闭)
❌ 没有 IP 白名单的 API key 用于实盘
❌ 没有 2FA 就开始实盘
❌ Phase 0 用 > 3x 杠杆(P0 阶段保守,先用 3x)
❌ 跳过 paper trading 直接实盘
❌ 资金阶梯跳级
```

---

## 紧急情况协议

**用户说"暂停"** → 立刻停下,无需问理由
**用户说"撤销上一步"** → 把刚改的回滚,然后停下来确认
**用户连续 2 次说"不要废话"** → 切到极简模式(只回复关键信息)
**bug 涉及资金安全** → 立刻 CRITICAL 通知 + 停所有交易,等待人工

---

## 当前阶段任务(2026-05-06 起)

### 接下来要做的事

```
立刻:Week 1-2 项目骨架

第 1 步:目录结构 + pyproject.toml
第 2 步:docker-compose.yml
第 3 步:数据库 schema + alembic
第 4 步:配置加载 + 日志
第 5 步:健康检查脚本
```

**不要做的事**:
- 不要写策略代码
- 不要写交易代码
- 不要写订单代码
- 不要批量出文件

---

## 文档维护

每次重要改动:
1. 改对应的 docs/*.md
2. 更新 docs/CHANGELOG.md(在最上面加 entry)
3. 必要时更新 docs/HANDOFF.md

---

## 联系协议

如果你(Claude Code)对某件事不确定:

```
1. 先看 docs/REVIEW_RESULT.md(用户的决策记录)
2. 再看 docs/01_overview_and_architecture.md(原始设计)
3. 还不确定,直接问用户:"我想做 X,但有 Y 顾虑"
4. 用户回答后,做并继续
```

不要默默做出"打破设计"的决定。

---

**版本**:v1.0 · 2026-05-06
**对应**:HANDOFF.md v1.0
