# 部署审计 2026-05-13 — HTX 筛选器手动应用 + #02 回测 UI 候选化

## 部署人 / 时间
- 触发：用户（YANG）授权 B 方案
- 实施：Claude session，2026-05-13 ~05:00 BJT
- 部署模式：前后端一起 rebuild

## 变更范围（我的改动 = 4 后端 + 1 前端 + 1 新文件 + 1 yaml）

| 文件 | 性质 | 摘要 |
|---|---|---|
| `backend/app/strategies/perp_basis/htx_screener_runner.py` | 新增 | `HTXScreenerRunner` 类：按需 run_now() + apply_to_runtime() |
| `backend/app/api/v1/scanner.py` | 改 | 加 `warm_cache()` + 3 个端点 `GET /htx-premium/status`、`POST /run`、`POST /apply` |
| `backend/app/main.py` | 改 | lifespan 仅 instantiate runner（不起周期 task），写入 `app.state.htx_screener_runner` |
| `backend/app/services/runtime_overrides.py` | 改 | `_PERP_BASIS_ALLOWED` 加 `candidate_symbols` + `stop_price_divergence_pct`；`apply_to_perp_basis_cfg()` 处理 candidate_symbols |
| `config/strategies/perp_basis_main.yaml` | 改 | 加 `screener: {enabled, days}` 块（精简后） |
| `frontend/app/(dashboard)/backtest/page.tsx` | 改 | #02 回测 UI 改造：删 PB_SYMBOL_GROUPS / PB_ALL_SYMBOLS / filterMode / toggleGroup；币种列表完全由 `liveConfig.candidate_symbols` 驱动 |

## 一起被打包的前任未提交改动（32 处）
- 后端 18 modified + 2 untracked（含 notification_credentials.py / onchain_balance.py）
- 前端 12 modified
- 我无法独立隔离这些改动（与我的改动文件混合）；用户在前几次会话留下，假设已 review

## 回滚锚点
- **镜像备份 tag**：`dracula-api:rollback-2026-05-13` + `dracula-frontend:rollback-2026-05-13`
- **yaml 备份**：`config/strategies/perp_basis_main.yaml.bak.2026-05-13`
- **overrides 备份**：`state/overrides.json.bak.2026-05-13`
- **持仓快照（重启前）**：
  - APE/USDT uuid=10adf637, opened=2026-05-12T15:30:36Z, funding=0.115, fees=0.161
  - ENJ/USDT uuid=4366bbd9, opened=2026-05-12T13:36:48Z, funding=0.183, fees=0.157
  - INJ/USDT uuid=26f4b810, opened=2026-05-12T13:28:05Z, funding=0.115, fees=0.159

## 回滚命令（如出错）

```bash
# 镜像层回滚
ssh dracula
cd /opt/dracula
sudo docker tag dracula-api:rollback-2026-05-13 dracula-api:latest
sudo docker tag dracula-frontend:rollback-2026-05-13 dracula-frontend:latest
sudo docker compose up -d api frontend

# 如果配置层也要回退（yaml/overrides 被错改了）
sudo cp config/strategies/perp_basis_main.yaml.bak.2026-05-13 config/strategies/perp_basis_main.yaml
sudo cp state/overrides.json.bak.2026-05-13 state/overrides.json
sudo docker compose restart api
```

## Smoke test 检查清单
- [ ] `dracula-api-1` healthy（compose ps）
- [ ] `GET /api/v1/health` 200
- [ ] `GET /api/v1/strategies/perp-basis/config` 返回 candidate_symbols
- [ ] `GET /api/v1/positions?strategy_instance=perp_basis_main&status=open` 仍 3 笔，UUID 不变
- [ ] `GET /api/v1/scanner/htx-premium/status` 返回 `available: true`
- [ ] `POST /api/v1/scanner/htx-premium/run` 30-90s 成功（或返回 cache 命中）
- [ ] `dracula-frontend-1` 启动
- [ ] 前端 /backtest 打开 #02 区域只显示 13 个候选（无 PYTH/RNDR/PEPE 等）

## 验证步骤未通过时的判断
- API health failed → 看 `docker logs dracula-api-1 --tail 200` → 若是 lifespan 初始化失败立即回滚
- 持仓 UUID 变了或丢失 → 严重，立即回滚 + 排查 paper_trading.restore()
- htx_screener_runner 不可用 → 非致命，可暂时不回滚（功能未上线影响小）
- 前端报错 → 单独回滚 frontend，保留 api 新版


---

## 第二次部署 2026-05-13 ~05:45 BJT — HTX 筛选器 Widget UI

### 增量变更
| 文件 | 改动 |
|---|---|
| `backend/app/strategies/perp_basis/htx_screener_runner.py` | `apply_to_runtime()` 加 `mode` 参数（replace / union），重写算法 |
| `backend/app/api/v1/scanner.py` | `HTXApplyBody` 加 `mode` 字段；protect 持仓改走 DB 查询（绕过 app.state.perp_basis_paper 进程间访问限制） |
| `frontend/lib/api/scanner.ts` | 新建：3 个 endpoint 的 TS client + types |
| `frontend/app/(dashboard)/strategies/[id]/page.tsx` | 新增 `HTXScreenerCard` 组件 + 插入 perp-basis 配置 Card 之后 |

### 修复的 bug
1. `frontend` build error `opps unused` — 删除多余 IIFE
2. `PositionRecord.symbol` 不存在 — symbol 实际存在 `notes` 字段（见 backend/app/models/position.py:75）
3. `app.state.perp_basis_paper` 跨进程访问空指针 — 改走 DB

### 新回滚锚点
- `dracula-api:rollback-2026-05-13-v2` （第一次部署后版本）
- `dracula-frontend:rollback-2026-05-13-v2`

### 第二次部署的回滚命令
```bash
ssh dracula
cd /opt/dracula
sudo docker tag dracula-api:rollback-2026-05-13-v2 dracula-api:latest
sudo docker tag dracula-frontend:rollback-2026-05-13-v2 dracula-frontend:latest
sudo docker compose up -d api frontend
```

### 端到端 smoke test 通过
- ✅ `POST /scanner/htx-premium/run` 同步跑 107s，205 symbols → STRONG=3 (COMP/ENJ/APE) + MODERATE=5
- ✅ `POST /scanner/htx-premium/apply {mode:"union"}` 应用，protect=[APE,ENJ,INJ] DB 拿到
- ✅ candidate_symbols 仍 13 个（noop union 因为 screener picks 全在）
- ✅ 3 笔持仓 UUID 不变（10adf637/4366bbd9/26f4b810）
- ✅ scanner 继续 ticking，top_diff_apr 113.41% ENJ/USDT
- ✅ /strategies/perp-basis 页 chunk hash 更新 `page-5e00a1db4af2f5b0.js`

---

## 第三次部署 2026-05-13 ~04:09 BJT — 修 reconciler 误报 + HTX close 路径

### 触发
- 03:45:47 STABLE/USDT 单腿暴露 critical 告警 → auto_close 调 HTX 失败
- 用户审查 7d risk_events：19 次 single_leg_exposure 全部误报，0 真实裸腿
- 共识：必须同步修两处 bug，不能分别部署

### 改动
| 文件 | 改动 |
|---|---|
| `backend/app/services/balance_reconciler.py` | (1) 新常量 `_RECONCILE_OPEN_GRACE_SECONDS = 60.0`；`_check_leg` 跳过开仓后 60s 内的 perp leg 检查（防 binance API 短暂未同步引起的 false positive）；(2) `_try_auto_close_remaining_leg` 加 `reduceOnly=True` 参数 + symbol 自动加 `:USDT` 后缀（CCXT swap unified 格式），让 ccxt 走 perp close 路径而不被误判为 spot trade |

### 回滚锚点
- `dracula-api:rollback-2026-05-13-v3`（第三次部署前版本）

### Smoke test
- ✅ api healthy；5 笔持仓 UUID 不变（INJ/ENJ/APE/SKYAI/STABLE）
- ✅ scanner / paper / reconciler / spot_perp / cex_dex 5 个 runner 全部 started
- ✅ paper_basis 重启日志 min_diff=75 notional=100 与配置一致

### 验证标准（24h 后）
- single_leg_exposure 误报率应大幅下降
- 若有真实裸腿，HTX auto close 应成功（reduceOnly + 正确 symbol 走 perp 路径）

### 完整回滚命令
```bash
sudo docker tag dracula-api:rollback-2026-05-13-v3 dracula-api:latest
sudo docker compose up -d api
```

---

## 第四次部署 2026-05-13 ~04:42 BJT — Binance perp 默认 margin mode: isolated → cross

### 触发
- 用户实战感知：isolated 模式下每仓 margin 独立，cross 余额仅 $10 救不了
- 当前 binance 3 仓全 isolated，wallet $96 大部分锁在 isolated bucket
- OKX 端已是 cross/10x 对照

### 改动
- backend/app/execution/live_broker.py:642 `set_margin_mode("isolated", ccxt_sym)` → `set_margin_mode("cross", ccxt_sym)`
- 同行 docstring 更新为 CROSS

### 行为
- 当前 5 个持仓不动（binance 持仓中拒改 margin mode，except 已吃错）
- 未来新 symbol 仓位 → cross
- 老 symbol 平仓后再开 → cross

### 回滚锚点
- dracula-api:rollback-2026-05-13-v4

### Smoke test
- 5 笔持仓 UUID 不变（INJ/ENJ/APE/SKYAI/STABLE）
- 6 个 runner 全部 started
- API healthy

### 回滚命令
sudo docker tag dracula-api:rollback-2026-05-13-v4 dracula-api:latest
sudo docker compose up -d api

---

## 第六次部署 2026-05-13 ~17:49 BJT — entry 价差预检 + stop 调整

### 触发
- 上午 ENJ 反复开仓循环（5min 8 笔，烧 -$13.5）
- 根因：yaml max_entry_price_divergence_pct: 0.7 实际 dead-config (整个 backend 0 引用)
- 用户先 PATCH stop=50 关掉，再 PATCH 调回 5.0
- 决策：实现 entry 价差预检 + stop 阈值 5%

### 改动 4 个文件
- paper_trading.py: __init__ 加 max_entry_price_divergence_pct 参数；_open_cross_exchange 拿到 long_price/short_price 后做预检：|long-short|/mid*100 >= 阈值 → skip + info log
- session_factory.py: 从 yaml risk.max_entry_price_divergence_pct 读取传给 paper_session
- runtime_overrides.py: _PERP_BASIS_ALLOWED 加 max_entry_price_divergence_pct + apply_to_perp_basis_cfg 分支
- api/v1/strategies.py: PATCH handler 同步 paper._max_entry_price_div 热重载
- yaml: max_entry_price_divergence_pct 0.7 → 2.0 (与用户认知对齐)

### 回滚锚点
- dracula-api:rollback-2026-05-13-v6

### 实证验证
- ENJ 当前 OKX 0.04696 vs HTX 0.04666 价差 0.64% (之前出事时 2.1%) → 允许开仓
- APE 当前 OKX 0.14893 vs HTX 0.14880 价差 0.09% → 允许开仓
- 假如 ENJ 再次扩大到 2%+ 价差 → 自动 skip + info log

### Smoke test
- 5 笔双腿对账：剩 2 笔 (STABLE/APE) — 早晨手动平了 ENJ 裸腿 (-$1.48)
- circuit_breaker daily_dd -3.89% 触发暂停新开仓 (今日亏损过 3%)
- scanner ticking 正常 top SKYAI 158%
- yaml max_entry=2.0 + stop=5.0 双层保护
